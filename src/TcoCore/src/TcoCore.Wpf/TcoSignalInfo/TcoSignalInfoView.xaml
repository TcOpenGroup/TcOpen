<UserControl x:Class="TcoCore.TcoSignalInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TcoCore" 
             xmlns:tcocore="clr-namespace:TcoCore;assembly=TcoCoreConnector" xmlns:vortex="http://vortex.mts/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <tcocore:TcoSignalInfo></tcocore:TcoSignalInfo>
    </d:UserControl.DataContext>
    <GroupBox Header="{Binding Converter={local:NameOrSymbolConverter}}">
        <UniformGrid Rows="0" Columns="2">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource MaterialDesignBody2TextBlock}" Text="{Binding Signal.HumanReadable}"></TextBlock>
            <vortex:RenderableContentControl DataContext="{Binding Signal}" PresentationType="DisplaySlim"></vortex:RenderableContentControl>
        </UniformGrid>
    </GroupBox>
</UserControl>
