<UserControl
    x:Class="TcoCore.TcoTaskView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:TcoCore"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vortex="http://vortex.mts/xaml"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <d:UserControl.DataContext>
        <local:TcoTaskViewModel />
    </d:UserControl.DataContext>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80*" />
                    <ColumnDefinition Width="20*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Button Grid.Column="0" Command="{Binding TcoTask}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MaxWidth="200" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding TcoTask, Converter={local:NameOrSymbolConverter}}"
                            TextWrapping="WrapWithOverflow" />
                        <iconPacks:PackIconEntypo
                            Grid.Column="1"
                            Margin="20,0,0,0"
                            Kind="ControllerPlay" />
                    </Grid>
                </Button>
                <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" 
                             Value="{Binding TcoTask._taskState.Cyclic, Converter={local:TaskStateToProgressConverter}}" 
                             IsIndeterminate="{Binding TcoTask._taskState.Cyclic,Converter={local:TaskStateToBoolConverter}}" Margin="0,5,0,0"/>
                <Button Grid.Column="1" Command="{Binding RestoreCommand}">
                    <iconPacks:PackIconModern Kind="Reset" />
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>