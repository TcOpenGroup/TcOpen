<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainPlc"
             xmlns:vortex="http://vortex.mts/xaml" 
             x:Class="MainPlc.TcoTaskedSequencerSequenceView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <local:TcoTaskedSequencerTaskViewModel/>
    </d:UserControl.DataContext>
    <Grid>
        <Grid Grid.Row="0" Visibility="{Binding Component._task._taskState.Cyclic, Converter={local:TaskStateToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <vortex:RenderableContentControl Grid.Row="0" DataContext="{Binding Component}" PresentationType="Base"/>
            <vortex:RenderableContentControl Grid.Row="1" DataContext="{Binding Component._o}"/>
        </Grid>
    </Grid>
</UserControl>
